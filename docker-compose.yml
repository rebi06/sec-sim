services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: secsim
      POSTGRES_PASSWORD: secsim
      POSTGRES_DB: secsim
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
